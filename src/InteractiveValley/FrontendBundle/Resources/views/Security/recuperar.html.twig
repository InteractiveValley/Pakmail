{% extends 'FrontendBundle::pakmail.html.twig' %}

{% block title %}Pakmail | Recuperar contraseña{% endblock %}

{% if error is not defined %}
    {% set error = false %}
{% endif %} 

{% block body %}
    <header class="vino">
        <div class="text-align-center">
            <figure class="logo no-margin">
                <a href="{{path('homepage')}}"><img src="{{asset('images/logo_footer.png')}}"></a>
            </figure>
        </div>
    </header>
    <!-- Form area -->
    <div class="admin-form">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <!-- Widget starts -->
                    <div class="widget">
                        <!-- Widget head -->
                        <div class="widget-head">
                            <i class="icon-lock"></i> Recuperar contraseña
                        </div>
                        <div class="widget-content">
                            <div class="padd contenido-formulario">
                                <!-- Login form -->
                                <form class="form-horizontal" action='{{ path('pakmail_recuperar') }}'  method="post">
                                    <!-- Email -->
                                    <div class="form-group">
                                        <label class="control-label col-lg-3" for="email">Email</label>
                                        <div class="col-lg-9">
                                            <input type="email" id="email" class="form-control" name="email" value="" placeholder="Email">
                                        </div>
                                    </div>
                                    <div class="col-lg-9 col-lg-offset-3">
                                        <button type="button" class="btn btn-primary" onclick="history.back(1);">Regresar</button>
                                        <button type="submit" class="btn btn-danger">Recuperar contraseña</button>
                                    </div>
                                    <br />
                                </form>
                            </div>
                        </div>
                        <div class="widget-foot">
							{% for flashMessage in app.session.flashbag.get('notice') %}
								<div class="flash-info label label-info">
									{{ flashMessage }}
								</div>
							{% endfor %}
                            {% for flashMessage in app.session.flashbag.get('error') %}
								<div class="flash-error label label-danger">
									{{ flashMessage }}
								</div>
							{% else %}
								<br/>
							{% endfor %}
                        </div>
                    </div>  
                </div>
            </div>
        </div> 
    </div>
    <footer class="footer">
        <div class="container">
            <!--img src="{{asset('images/interactivevalley.png')}}"/-->
			&nbsp;
        </div>
    </footer>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function () {
            //$("body").css({'paddingTop': '30px'});
            $("footer").css({'position': 'fixed', 'bottom': '0px', 'width': '100%'});
            $(".admin-form").css({'margin': '0px auto'});
        });
    </script>
{% endblock %}   